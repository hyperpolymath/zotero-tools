# eclexiaiser manifest — energy/carbon resource budgets
# SPDX-License-Identifier: MPL-2.0

[project]
name = "rescript-templater"

# [[functions]] blocks mountApp + mountWithValidation removed
# 2026-05-26: their `source` pointed at examples/SafeDOMExample.res,
# which is being deleted estate-wide (gitbot-fleet#208 SafeDOM sweep —
# stale over-propagated ReScript fixture). Restore (with
# `source = "examples/SafeDOMExample.affine"` or similar) once
# affinescript#56 lands a canonical .affine equivalent.

[carbon]
provider = "static"
region = "GB"
static-intensity = 200.0

[report]
format = "text"
include-recommendations = true
